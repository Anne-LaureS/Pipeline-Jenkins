---
- name: Baseline de laboratoire TP7
  hosts: web_lab
  become: true
  tasks:
    - name: Installer curl
      ansible.builtin.package:
        name: curl
        state: present

    - name: Créer le répertoire de preuve
      ansible.builtin.file:
        path: /etc/training
        state: directory
        mode: '0755'

    - name: Déposer le fichier de preuve
      ansible.builtin.copy:
        dest: /etc/training/provisioned-tp7.txt
        content: |
          Configuré par Ansible (TP7 - projet global)
          Playbook: playbooks/site.yml
          Groupe: web_lab
        mode: '0644'
